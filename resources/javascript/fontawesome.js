import { library, dom } from '@fortawesome/fontawesome-svg-core';
import {
    faLanguage,
    faSearch
} from '@fortawesome/free-solid-svg-icons';

library.add(
    faLanguage,
    faSearch
);

dom.watch();
